# Headquaters AngularJS Client

## Konfiguracja
Strona stoi na nodejs więc żeby ją postawić należy: 
* Pobrać manualnie nodejs z https://nodejs.org 5.8.0 (a przynajmniej w mojej wersji ubuntu nie ma go w repo).
* Dodać do zmiennej PATH katalog nodejs/bin
* przejść do katalogu głównego projektu i wykonać:
  * ```npm install -g npm@latest grunt-cli```
  * ```npm install```
  * ```bower install```
  * ```grunt serve```

## Testy
```grunt test```
